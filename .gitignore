# Секреты — не должны попадать в публичный репозиторий
.env

# Локальные настройки Claude Code (у каждого свои)
.claude/settings.local.json

# Стандартный мусор Node.js (пригодится, когда появится бэкенд)
node_modules/
